# =====================================================================
#  Sunrise Dental Clinic - TIER 2 configuration
#
#  The datasource is commented out for now. Unit tests in this TDD cycle
#  test pure domain logic and need no database, so the application can be
#  developed and tested before MySQL is wired in.
#  Uncomment these once the repository layer arrives.
# =====================================================================

server.port=8080
spring.application.name=sunrise-clinic-service

# spring.datasource.url=jdbc:mysql://localhost:3306/sunrise_clinic?serverTimezone=Asia/Colombo
# spring.datasource.username=root
# spring.datasource.password=CHANGE_ME
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=true
